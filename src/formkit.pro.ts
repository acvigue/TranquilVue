import { createProPlugin, slider, toggle, dropdown, repeater } from '@formkit/pro'
const proPlugin = createProPlugin('fk-6729b314b8', {
  slider,
  toggle,
  dropdown,
  repeater
})

export default proPlugin
